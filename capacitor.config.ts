import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.projectpbp',
  appName: 'projectpbp',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
